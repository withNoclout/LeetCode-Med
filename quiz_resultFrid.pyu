class Solution(object):
    def minimumTimeToInitialState(self, word, k):
        n = len(word)
        for i in range(k, n, k):
            if word[i:] == word[:n-i]:
                return i // k
        return (n + k - 1) // k
